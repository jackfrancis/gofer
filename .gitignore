/bin/
/build/
*.out
*.test
*.tar
.env
# The generated run-credential keypair: public half -> gofer secret (verify-only);
# the private half a runtime's minter would hold is unused here. Kept out of git.
.gofer-agent-key.env
/server
