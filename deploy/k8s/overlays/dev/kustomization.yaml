# Dev overlay for `make dev-up`. The web app needs no dispatch registration, so the
# overlay is just the application base. A concrete agent-runtime backend adds its own
# registration here (e.g. a CR binding gofer's ServiceAccount to a runtime/provider).
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
