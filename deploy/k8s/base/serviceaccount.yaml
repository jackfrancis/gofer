# The identity the web Deployment runs as. No Kubernetes RBAC is bound to it: the web
# tier creates no per-run object and needs no Job/Pod privilege. A concrete
# agent-runtime backend uses this SA's projected token to authenticate its dispatch to
# the runtime's control plane.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: gofer
  namespace: gofer
